import { NumberDeserializer } from './number-deserializer';

describe('NumberDeserializer', () => {
  it('should create an instance', () => {
    expect(new NumberDeserializer()).toBeTruthy();
  });
});
