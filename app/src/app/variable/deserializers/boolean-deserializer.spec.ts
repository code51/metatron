import { BooleanDeserializer } from './boolean-deserializer';

describe('BooleanDeserializer', () => {
  it('should create an instance', () => {
    expect(new BooleanDeserializer()).toBeTruthy();
  });
});
