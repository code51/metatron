import { ShortStringDeserializer } from './short-string-deserializer';

describe('ShortStringDeserializer', () => {
  it('should create an instance', () => {
    expect(new ShortStringDeserializer()).toBeTruthy();
  });
});
